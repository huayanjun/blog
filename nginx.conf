worker_processes auto;
events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        server_name blog.huayanjun.xyz;
        root /usr/share/nginx/html;
        index  index.html;
    }
}